# PostgreSQL Client Authentication Configuration File
# Replica Server Configuration

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
host    all             all             0.0.0.0/0               md5

# IPv6 local connections:
host    all             all             ::1/128                 trust

# WireGuard network ranges for multi-site access
# Ravena, NY (primary) - 10.0.1.0/24
host    all             all             10.0.1.0/24             md5

# Burlington, VT - 10.0.2.0/24  
host    all             all             10.0.2.0/24             md5

# Montpelier, VT - 10.0.3.0/24
host    all             all             10.0.3.0/24             md5

# Nantucket, MA - 10.0.4.0/24
host    all             all             10.0.4.0/24             md5

# Tucson, AZ - 10.0.5.0/24
host    all             all             10.0.5.0/24             md5

# Docker network ranges
host    all             all             172.16.0.0/12           md5

# Application connections (read-only)
host    showbuild       showbuild       0.0.0.0/0               md5